# ─── Web Framework ───────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ─── Base de Datos ───────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# ─── Cache y Colas ───────────────────────────────────────────
redis==5.0.4
celery==5.4.0
flower==2.0.1

# ─── Autenticación ───────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# ─── HTTP y Scraping ─────────────────────────────────────────
httpx==0.27.0
praw==7.7.1
google-api-python-client==2.130.0
feedparser==6.0.11
# newspaper3k removido — feedparser ya cubre RSS/noticias sin compilar lxml desde fuente

# ─── NLP (modo API — sin modelos locales) ────────────────────
# NLP_MODE=api usa HuggingFace Inference API vía httpx (no requiere torch/transformers local).
# Para NLP_MODE=local agregar al .env y descomentar:
#   transformers==4.41.2
#   torch==2.3.0        ← ~2 GB, solo necesario para modo local
langdetect==1.0.9
# lingua-language-detector removido — compila Rust desde fuente (~40 min). langdetect cubre el mismo caso.
wordcloud==1.9.3

# ─── Reportes PDF ────────────────────────────────────────────
weasyprint==62.3
pydyf==0.11.0           # pydyf 0.12+ rompe WeasyPrint 62.x (incompatible API)
jinja2==3.1.4
matplotlib==3.9.0

# ─── Utilidades ──────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1
sse-starlette==2.1.0
pillow==10.3.0

# ─── Facebook / Instagram (scraping autenticado) ─────────────
instagrapi>=2.0.0         # Instagram scraping con cuenta propia (sin API oficial)
facebook-scraper>=0.2.59  # Facebook búsqueda por keyword con cookies de sesión
lxml-html-clean             # lxml.html.clean separado como paquete independiente en lxml>=5

# ─── Phase 2 ─────────────────────────────────────────────────
twscrape>=0.14.6          # Scraper Twitter/X (sin API key oficial)
xclienttransaction>=1.0.2  # Genera x-client-transaction-id requerido por Twitter GraphQL
# playwright>=1.44.0      ← Solo en Dockerfile.facebook-worker (worker local)
# TikTokApi>=6.0.0        ← Solo en Dockerfile.facebook-worker (worker local)

# ─── v2: IA / Agentes ────────────────────────────────────────
groq>=0.9.0               # LLM gratuito: Llama 3.1 vía Groq API (14,400 tok/min free tier)
scikit-learn>=1.4.0       # Topic modeling (TF-IDF + KMeans) y clasificación bots (Módulos 1.1, 2.1)
statsmodels>=0.14.0       # Suavizado exponencial Holt-Winters para pronóstico de tendencias (Módulo 4.2)
spacy>=3.7.0,<4.0.0       # NER: extracción de entidades nombradas PER/ORG/LOC (Módulo 1.2)
pgvector>=0.3.0           # Tipo vector + operadores coseno en SQLAlchemy/PostgreSQL (Módulo 6.1)
geopy>=2.4.0              # Geocodificación texto → country_code con Nominatim (gratuito, sin API key)

# ─── Módulo 7: Reconocimiento Visual ────────────────────────────────────────
# Desactivado en producción (FACE_RECOGNITION_ENABLED=false).
# Para activar: descomentar, agregar cmake/libopenblas-dev al Dockerfile.prod y rebuildar.
dlib>=19.24.0
face-recognition>=1.3.0
requests>=2.32.0          # Descarga de imágenes desde URLs de menciones

# ─── Módulo 8: Búsqueda Inversa de Imágenes ─────────────────────────────────
imagehash>=4.3.1          # pHash/dHash perceptual — depende de pillow y scipy (ya instalados)
opencv-python-headless>=4.9.0  # extracción de frames de video para Módulo 8
