# Dockerfile para CI: el npm install y npm run build se ejecutan FUERA de Docker
# (en el runner de GitHub Actions). Este Dockerfile solo copia el dist/ ya compilado.
FROM nginx:alpine
COPY dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
